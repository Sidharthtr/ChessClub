{
  "name": "chessclub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix backend && npm run lint:fix --prefix frontend",
    "format": "prettier --write \"**/*.{ts,tsx,js,cjs,mjs,json,css,md}\" --ignore-path .prettierignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,cjs,mjs,json,css,md}\" --ignore-path .prettierignore",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.4"
  }
}
